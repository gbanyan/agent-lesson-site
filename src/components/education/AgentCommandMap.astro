<figure class="visual relation-map">
  <figcaption class="visual-label">列出照片資料夾時，工作怎麼發生</figcaption>
  <div class="relation-flow" aria-hidden="true" data-pagefind-ignore>
    <span>拿到：你要求查看 「照片」裡的項目</span><b>↓</b>
    <span>AI 模型根據目前資訊決定下一步</span><b>↓</b>
    <span>操作：工具執行列出項目的指令</span><b>↓</b>
    <span>程式實際工作，讀取資料夾</span><b>↓</b>
    <span>結果：檔名清單回到 Agent，原檔未改</span>
  </div>
  <p class="visually-equivalent sr-only">你要求查看 「照片」裡的項目，AI 決定操作，工具執行列出項目的指令，程式讀取資料夾。檔名清單回到 Agent，原檔未改。</p>
  <p class="terminal-note">終端機是輸入指令與查看結果的介面，不是 Agent 本身。</p>
</figure>
<style>
  .relation-flow { display:grid; justify-items:center; gap:.45rem; max-width:34rem; margin:auto; text-align:center; }
  .relation-flow span { width:100%; padding:.7rem 1rem; border:1px solid var(--line); border-radius:var(--radius-sm); background:var(--surface-soft); }
  .relation-flow b { color:var(--ink-soft); }
  .terminal-note { margin:1rem 0 0; padding:.8rem 1rem; border-left:4px solid var(--blue); background:var(--surface-soft); }
</style>
