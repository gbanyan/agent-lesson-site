---
import { uid } from '../../lib/uid';
interface Props { text: string; }
const { text } = Astro.props;
const titleId = uid('takeaway-', `${Astro.url.pathname}|${text}`);
---
<aside class="takeaway" aria-labelledby={titleId}>
  <strong id={titleId}>這頁重點</strong>
  <p>{text}</p>
</aside>
